import React from "react";

import CustomLayout from "../Layout";

const Tours = () => {
  return (
    <CustomLayout>
      <div className="content"></div>
    </CustomLayout>
  );
};

export default Tours;
